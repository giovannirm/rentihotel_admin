from django.urls import path
from rest_framework.routers import DefaultRouter
from .views import *

app_name = 'servicio'

#router = DefaultRouter()
#router.register('service', ServiceViewSet ,basename='service_administration')

urlpatterns = [
    
]
#urlpatterns += router.urls